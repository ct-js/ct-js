move ct.fps, ct.speed, ct.highDensity to ct.render.[...]
TODO: add getters/setters for easier control over game's rendering

remove ct.u.ext/ct.u.extend

move CtAction -> ct.Action

move ct.templates.Tilemap -> ct.Tilemap

TODO: this.zIndex -> this.zIndex, with fallbacks
    [ ] Background
    [x] Copy
    [ ] TileLayer

New: ct.backgrounds.list.OTHER for backgrounds created with custom textures

New: unloadAtlas, unloadBitmapFont

ct.res.loadBitmapFont -> `name` argument is no longer needed.
