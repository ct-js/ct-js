emitter-tandem-editor, [data-is="emitter-tandem-editor"]
    & > .aResizer, & > .flexfix
        flex 0 0 auto !important
    emitter-editor, .emitter-tandem-editor-anAddEmitterButton
        flex 0 0 auto !important
        width 20rem
    emitter-editor
        overflow-y scroll
    [ref="preview"]
        overflow hidden
    .emitter-tandem-editor-Tools, .zoom
        position absolute
        margin 0.5rem 1rem
    .emitter-tandem-editor-Tools
        bottom 0
        left 0
        right 0
    .zoom
        right 0
        top 0
    .aPanel
        border-radius 0
