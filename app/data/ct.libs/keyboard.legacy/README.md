This is a legacy version of `ct.keyboard` that does not support Actions.

This module registers all the keyboard events in the game and allows your code to listen button presses and handle inputed strings.