declare namespace ct {
    namespace htmlUi {
        /**
         * Adds a UI element.
         * 
         * @param element Needs docs
         * @param options Needs docs
         * @param events Needs docs
         */
        function addUIElement(element: any, options: any, events: any): any;

        /**
         * Needs docs
         */
        var overlay: Array;
    }
}